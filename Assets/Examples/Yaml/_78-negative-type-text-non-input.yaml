name: Negative Type Text On Non Input Element
description: |
  Verifies that type_text fails when applied to a non-editable element.
  This test is expected to FAIL.

fixture:
  host_window:
    type: UnityUIFlow.Examples.ExampleCoverageInputWindow
    reopen_if_open: true

steps:
  - name: type_text on a Label element should fail
    action: type_text
    selector: "#gesture-status"
    text: "should fail"
