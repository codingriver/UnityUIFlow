name: Negative Tab View Invalid Index
description: Expects failure when select_tab index is out of range.
fixture:
  host_window:
    type: ExampleCoverageCollectionsWindow
    reopen_if_open: true
steps:
  - action: select_tab
    selector: "#settings-tabs"
    index: "99"
